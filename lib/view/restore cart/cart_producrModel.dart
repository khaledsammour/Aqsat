/*class CartProductModel {
  late String name, img, price, productId;
  late int quantity;
  CartProductModel({
    required this.name,
    required this.img,
    required this.quantity,
    required this.price,
    required this.productId,
  });

  CartProductModel.fromJson(Map<dynamic, dynamic> map) {
    if (map == null) {
      return;
    }
    name = map['name'];
    img = map['img'];
    price = map['price'];
    quantity = map['quantity'];
    productId = map['productId'];
  }
  toJson() {
    return {
      'name': name,
      'img': img,
      'price': price,
      'quantity': quantity,
      'productId': productId,
    };
  }
}*/
